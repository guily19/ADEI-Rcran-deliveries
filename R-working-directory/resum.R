# Borrar variables del entorn
rm(list=ls())
rm(a)
# Declarar un vector
b=c(3,4,5)

# Crea n numeros random distribuits normalment rnorm(n)
rnorm(10)
# per donar tambe la mean i la standr desviation
rnorm(10, mean=1.2, sd=3.4)

# help i exemples
help(comanda)
example(comanda)

